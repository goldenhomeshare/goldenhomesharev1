import { Metadata } from "next";

export async function generateMetadata(): Promise<Metadata> {
  return {
    title: "View Homes | Golden HomeShare",
    description: "Browse available homes and rooms for shared living. Find affordable housing options with trusted homeowners in your area.",
  };
}

// ... existing code ... 